pipeline {
    agent any

    stages {
        stage("Current Branch") {
            steps {
                sh "git branch"
            }
        }

        stage("Build") {
            steps {
                sh "./gradlew clean build"
            }
        }

        stage("Dockerize") {
            steps {
                sh "docker build -t hello-kotlin:latest ."
                echo "Pushing to dockerhub..."
            }
        }


    }
}