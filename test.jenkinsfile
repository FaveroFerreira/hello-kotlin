pipeline {
    agent any

    stages {
        stage("Current Branch") {
            steps {
                sh "git branch"
            }
        }

        stage("Build") {
            steps {
                sh "./gradlew clean build"
            }
        }

        stage("Test") {
            steps {
                sh "ls"
            }
        }
    }
}