pipeline {
    agent any

    parameters {
        string(name: "BRANCH_NAME", defaultValue: "master", description: "Branch a ser construida!")
    }

    stages {
        stage("Say Hello") {
            steps {
                echo "Hello World!"
            }
        }

        stage("Build") {
            steps {
                echo "Building"
            }
        }

        stage("Test") {
            steps {
                echo "Testing..."
            }
        }
    }
}