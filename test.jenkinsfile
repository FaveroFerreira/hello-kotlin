pipeline {
    agent any

    stages {
        stage("Current Branch") {
            steps {
                sh "git branch"
            }
        }

        stage("Build") {
            steps {
                echo "Building"
            }
        }

        stage("Test") {
            steps {
                echo "Testing..."
            }
        }
    }
}